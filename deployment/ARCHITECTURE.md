# BodhiRAG Deployment Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    Hugging Face Space                       │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              Gradio Web Interface                     │ │
│  │  ┌─────────────────────────────────────────────────┐ │ │
│  │  │  User Query Input                               │ │ │
│  │  │  "What causes bone loss in space?"              │ │ │
│  │  └─────────────────────────────────────────────────┘ │ │
│  │                        ↓                              │ │
│  │  ┌─────────────────────────────────────────────────┐ │ │
│  │  │  Query Classification                           │ │ │
│  │  │  (KG Primary / VS Primary / Hybrid)             │ │ │
│  │  └─────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
│                        ↓                                    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │           Hybrid RAG Agent (Router)                   │ │
│  │                                                       │ │
│  │  ┌──────────────────┐    ┌──────────────────┐       │ │
│  │  │ KG Connector     │    │ VS Connector     │       │ │
│  │  │ (Neo4j)          │    │ (ChromaDB)       │       │ │
│  │  └──────────────────┘    └──────────────────┘       │ │
│  └───────────────────────────────────────────────────────┘ │
│           ↓                           ↓                     │
└───────────┼───────────────────────────┼─────────────────────┘
            ↓                           ↓
┌───────────────────────┐   ┌───────────────────────┐
│   Neo4j Database      │   │   ChromaDB Store      │
│   (External/AuraDB)   │   │   (Embedded)          │
│                       │   │                       │
│  • Entities           │   │  • Document Chunks    │
│  • Relationships      │   │  • Embeddings         │
│  • Evidence           │   │  • Metadata           │
│  • Sources            │   │  • Similarity Search  │
└───────────────────────┘   └───────────────────────┘
```

## Data Flow

### 1. Query Processing

```
User Query
    ↓
Intent Classification
    ↓
┌─────────────┴─────────────┐
│                           │
Relationship Query      Descriptive Query
    ↓                       ↓
Knowledge Graph         Vector Store
    ↓                       ↓
└─────────────┬─────────────┘
    ↓
Result Synthesis
    ↓
Answer + Sources
```

### 2. Knowledge Graph Retrieval

```
Query: "What causes bone loss?"
    ↓
Extract Entities: ["bone loss", "microgravity"]
    ↓
Neo4j Cypher Query:
MATCH (e:Entity {name: "Bone Loss"})-[r:RELATES_TO]->(target)
RETURN e, r, target
    ↓
Results:
- Microgravity → causes → Bone Loss
- Radiation → affects → Bone Loss
- Exercise → mitigates → Bone Loss
    ↓
Format with Evidence
```

### 3. Vector Store Retrieval

```
Query: "Describe oxidative stress"
    ↓
Generate Embedding (384-dim vector)
    ↓
ChromaDB Similarity Search
    ↓
Top-K Documents (k=5)
    ↓
Rank by Similarity Score
    ↓
Return with Metadata
```

## Deployment Architecture

### Local Development

```
┌─────────────────────────────────────┐
│         Developer Machine           │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Python 3.11                  │ │
│  │  - src/                       │ │
│  │  - deployment/                │ │
│  │  - requirements.txt           │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Local Neo4j (Optional)       │ │
│  │  Port: 7687                   │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

### Hugging Face Deployment

```
┌─────────────────────────────────────────────────────────┐
│              Hugging Face Infrastructure                │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │  Space Container (CPU)                            │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  Gradio App (app.py)                        │ │ │
│  │  │  Port: 7860                                 │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  BodhiRAG Components                        │ │ │
│  │  │  - HybridRAGAgent                           │ │ │
│  │  │  - KnowledgeGraphConnector                  │ │ │
│  │  │  - VectorStoreConnector                     │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  │                                                   │ │
│  │  ┌─────────────────────────────────────────────┐ │ │
│  │  │  ChromaDB (Embedded)                        │ │ │
│  │  │  /app/data/chroma_db                        │ │ │
│  │  └─────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────┘ │
│                        ↓                                │
│                  (External Connection)                  │
└────────────────────────┼────────────────────────────────┘
                         ↓
              ┌──────────────────────┐
              │  Neo4j AuraDB        │
              │  (Cloud)             │
              │  bolt://...          │
              └──────────────────────┘
```

### Docker Deployment

```
┌─────────────────────────────────────────────────────────┐
│              Docker Compose Stack                       │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │  bodhirag-app Container                           │ │
│  │  - Gradio Interface                               │ │
│  │  - BodhiRAG Components                            │ │
│  │  - ChromaDB                                       │ │
│  │  Port: 7860                                       │ │
│  └───────────────────────────────────────────────────┘ │
│                        ↓                                │
│              (Internal Network)                         │
│                        ↓                                │
│  ┌───────────────────────────────────────────────────┐ │
│  │  neo4j Container                                  │ │
│  │  - Neo4j Database                                 │ │
│  │  - APOC Plugin                                    │ │
│  │  Ports: 7474 (HTTP), 7687 (Bolt)                 │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  Volumes:                                               │
│  - neo4j_data (persistent)                              │
│  - chroma_data (persistent)                             │
└─────────────────────────────────────────────────────────┘
```

## Component Interactions

### Knowledge Extraction Pipeline

```
NASA Publications (CSV)
    ↓
Document Loader (DoclingLoader)
    ↓
Chunking (HybridChunker)
    ↓
Knowledge Extractor (LLM)
    ↓
┌─────────────┴─────────────┐
│                           │
Entities              Relationships
    ↓                       ↓
Neo4j Graph           Neo4j Graph
(Nodes)               (Edges)
```

### Query Execution Flow

```
1. User Input
   "What causes bone loss in space?"
   
2. Query Classification
   → Intent: "kg_primary" (relationship query)
   
3. Entity Extraction
   → Entities: ["bone loss", "space", "microgravity"]
   
4. Knowledge Graph Query
   MATCH (e:Entity)-[r:RELATES_TO]->(target)
   WHERE e.name IN ["Bone Loss", "Microgravity"]
   RETURN e, r, target
   
5. Vector Store Query (if hybrid)
   → Semantic search for context
   
6. Result Synthesis
   → Combine KG relationships + VS documents
   → Add source citations
   → Format answer
   
7. Display Results
   → Answer text
   → KG relationships
   → Source documents
   → Statistics
```

## Technology Stack

### Frontend
```
Gradio 4.0+
├── Interface Components
│   ├── Textbox (query input)
│   ├── Checkboxes (KG/VS toggle)
│   ├── Button (submit)
│   └── Markdown (results)
└── Theme: Soft (customizable)
```

### Backend
```
Python 3.11
├── LangChain 0.0.352
│   └── Document processing
├── Pydantic 2.6+
│   └── Schema validation
└── Sentence Transformers
    └── Embeddings
```

### Databases
```
Neo4j 5.0+
├── Graph Database
├── Cypher Query Language
└── APOC Procedures

ChromaDB 0.4.22
├── Vector Store
├── Cosine Similarity
└── Persistent Storage
```

### Deployment
```
Hugging Face Spaces
├── Gradio SDK
├── CPU Runtime
└── Environment Variables

Docker
├── Multi-container
├── Networking
└── Volumes
```

## Security Architecture

```
┌─────────────────────────────────────┐
│         User Browser                │
└─────────────────┬───────────────────┘
                  ↓ HTTPS
┌─────────────────────────────────────┐
│    Hugging Face Space (Public)      │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  Gradio Interface             │ │
│  │  (No Auth by default)         │ │
│  └───────────────────────────────┘ │
│                                     │
│  Environment Variables (Secrets):   │
│  - NEO4J_URI                        │
│  - NEO4J_USERNAME                   │
│  - NEO4J_PASSWORD                   │
└─────────────────┬───────────────────┘
                  ↓ Bolt Protocol (TLS)
┌─────────────────────────────────────┐
│    Neo4j AuraDB (Private)           │
│    - Authentication Required        │
│    - Encrypted Connection           │
└─────────────────────────────────────┘
```

## Scalability Considerations

### Vertical Scaling
```
Free Tier (CPU)
    ↓
Upgrade to Pro ($9/month)
    ↓
Upgrade to Enterprise
    ↓
Custom Hardware
```

### Horizontal Scaling
```
Single Instance
    ↓
Load Balancer
    ↓
Multiple Instances
    ↓
Auto-scaling
```

### Database Scaling
```
Neo4j Free Tier
    ↓
Neo4j AuraDB Professional
    ↓
Neo4j Enterprise (Clustering)
    ↓
Read Replicas
```

## Monitoring & Observability

```
┌─────────────────────────────────────┐
│         Application Layer           │
│  - Query logs                       │
│  - Error tracking                   │
│  - Performance metrics              │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│         Database Layer              │
│  - Neo4j query stats                │
│  - ChromaDB operations              │
│  - Connection pool                  │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      Infrastructure Layer           │
│  - HF Space logs                    │
│  - Resource usage                   │
│  - Uptime monitoring                │
└─────────────────────────────────────┘
```

## Deployment Workflow

```
Developer Machine
    ↓
1. Code Changes
    ↓
2. Local Testing
    ↓
3. Run deploy.bat
    ↓
Deployment Script
    ↓
4. Prepare Files
    ↓
5. Create HF Space
    ↓
6. Upload Code
    ↓
Hugging Face
    ↓
7. Build Container
    ↓
8. Install Dependencies
    ↓
9. Start Application
    ↓
10. Health Check
    ↓
Live Space ✅
```

---

**Built for NASA Space Apps Challenge 2025** 🚀
